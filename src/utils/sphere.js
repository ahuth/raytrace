export default function Sphere({ point, color, specular, lambert, ambient, radius }) {
  this.point = point;
  this.color = color;
  this.specular = specular;
  this.lambert = lambert;
  this.ambient = ambient;
  this.radius = radius;
}
